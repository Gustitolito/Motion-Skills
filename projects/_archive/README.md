# Projetos Arquivados

Esta pasta armazena projetos legados ou inativos.

- Os projetos movidos para cá são ignorados pelo TypeScript (`tsconfig.json: exclude`) e não são carregados no Remotion Studio.
- Isso previne que código legado quebre o `npm run check` do estúdio.
